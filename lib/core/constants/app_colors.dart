import 'package:flutter/material.dart';

/// Primary brand color used for the logo, buttons, and highlights.
/// Matches the seed color used in ColorScheme.fromSeed in main.dart.
const Color kBrandColor = Colors.deepPurple;